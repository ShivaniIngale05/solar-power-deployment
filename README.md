# Solar Power Prediction App

This is a **Streamlit app** for predicting solar power generation using a trained Gradient Boosting model.  

---

## 🔗 Streamlit App Link

Access the live app here: [Solar Power Deployment](https://solar-power-deployment-shivani.streamlit.app/)

---

## 🛠️ Files in this repository

- `app.py` – The main Streamlit app file  
- `final_gb_model.pkl` – Pretrained Gradient Boosting model  
- `final_model_data.csv` – Dataset used for the model  
- `requirements.txt` – Python dependencies for running the app  
- `solar.png` – Sample image used in the app  
- `README.md` – This file  

---

## 💻 How to Run Locally

1. Clone the repository:
   ```bash
   git clone https://github.com/ShivaniIngale05/solar-power-deployment.git
   cd solar-power-deployment
    ```
2. Install dependencies:
 ```bash
 pip install -r requirements.txt
   ```
3. Run the app
 ```bash

streamlit run app.py
```
